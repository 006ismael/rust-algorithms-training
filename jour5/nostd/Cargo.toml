[package]
name = "nostd"
version = "0.1.0"
edition = "2024"

[dependencies]
cortex-m = "0.7"
cortex-m-rt = "0.7"
rp2040-hal = "0.10" # Si vous visez le RP2040 (Pico)
panic-halt = "0.2"   # Un panic handler tout prêt
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"